# Verification results

**v0.1.1 release verification (2026-09-11):** the release package build passed all **229 tests** and produced the MSI, app-update ZIP and SHA-256 files. MSI extraction verified 488 payload files, per-user installation identity, two direct-launcher shortcuts, no PowerShell bootstrap/custom actions, and the bundled .NET Core/Desktop 10.0.11 runtime. The app extracted from the MSI passed hidden GUI and real CLI export smoke checks using that bundled runtime. The final revision hint is `e.g. RevB, B, ver1.3, v1.3.2, 1.3.2`, and the Gerber popup no longer repeats the revision reminder. Evidence: `.local/release-0.1.1-package.log` and `.local/release-0.1.1-msi-inspection/verification.txt`. Only isolated fixtures were used for verification.

Production description checks follow-up on **2026-09-11**: **229 tests pass** and the Release app build has zero warnings/errors in `.local/production-description-build`. The three generation checks now have explicit required-fields, BOM-vs-database and BOM-vs-layout names, all including OEPS Description. They require the BOM description column/value, compare descriptions against the same database PN/MPN pair, and read/compare the PCB's custom OEPS Description property even when hidden. Layout messages now identify the generated BOM as the comparison source. Configuration validation reuses the shared database/description validator; the description fix still validates PN/MPN independently so missing descriptions remain repairable.

Regressions cover missing/empty/invalid descriptions and columns, database description alternatives and absence, exact matching with outer-whitespace trimming, incomplete-BOM dependency messages, independent layout comparisons, hidden/escaped PCB descriptions, duplicate/malformed properties, rejection of the ordinary Description field as a substitute, renamed CSV labels, and continuing other exports after description failures. Automated log: `.local/production-description-tests.log`. The GUI smoke and real CLI production exports passed in `.local/production-description-ui-smoke`; report text verifies all three new names, description failures and green/red status markers. The report screenshots were visually inspected. The native smoke uses an empty schematic BOM; populated BOM/PCB description comparisons are covered by the automated fixtures. The MSI has not been rebuilt for this follow-up.

OEPS Description fix follow-up on **2026-09-11**: **223 tests pass**; the Release app build has zero warnings/errors in `.local/description-fix-build`. `ConfigureFiles/OepsDescriptionFix.cs` now prepares description-only changes for CLI BOM components whose saved OEPS PN/MPN match a database pair with one usable description. `Kicad/SchematicDescriptionDocument.cs` follows sheet instances and changes only placed-symbol description values or inserts hidden description properties. Tests cover main/child sheets, repeated sheets, multiple symbol units, invalid/ambiguous pairings and descriptions, partial fixes, declined/cancelled approvals, stale project/schematic inputs, exact preservation of existing property formatting and UTF-8 BOM, escaped text, unrelated/library/excluded symbols, duplicate references, and unsafe/missing/cyclic sheet paths.

Real KiCad **10.0.3** verification passed in `.local/description-fix-reference`. Declining preserved schematic bytes; approval updated **29 components in four schematic files**, kept byte-for-byte original backups, and passed a fresh CLI identifier/description check. Running the fix again made no changes or prompts. Only isolated copies were modified. Evidence: `.local/description-fix-reference.log` and `.local/description-fix-tests.log`.

The hidden GUI smoke passed in `.local/description-fix-ui-smoke`, including the scrollable description proposal with visible Yes/No buttons, default No, both confirmation results, existing configuration/fix flows, report indicators and real production exports. `description-fix-confirmation.png` was visually inspected. The new dialog keeps the existing 620-pixel client width and OEPS styling. The MSI has not been rebuilt for this follow-up.

OEPS Description follow-up on **2026-09-11**: **213 tests pass** and the Release app build has zero warnings/errors in `.local/bom-description-build`. The existing CLI configuration check now explicitly exports `OEPS Description`, requires a nonempty valid value, and compares it with database `Description` for the same OEPS PN/MPN pair. Differences show the current and expected descriptions; missing database descriptions fail validation. Comparisons trim outer whitespace and otherwise preserve case, punctuation and internal spacing. There is no automatic fix. Regression cases cover missing/mismatched descriptions, case/spacing, quoted commas, database alternatives, unavailable descriptions, invalid/unresolved content, combined identifier errors, and the unchanged no-fix behavior.

Real KiCad **10.0.3** verification on `.local/bom-description-reference` (an isolated copy of the main schematic and three linked sheets) correctly reported missing descriptions and database mismatches, including C2/C7/C16 with outdated capacitor descriptions and J4 with different internal spacing. Hashes confirmed all copied project/schematic files were unchanged by the check. Automated log: `.local/bom-description-tests.log`. The MSI has not been rebuilt for this follow-up.

CLI configuration-check follow-up on **2026-09-11**: **208 tests pass**. Release builds of the app and test harness have zero warnings/errors. The new `CheckFilesConfiguration/SchematicBomIdentifiersCheck.cs` requests Reference, both OEPS PN aliases and MPN in a temporary CLI BOM, independently of saved table visibility, labels, grouping and CSV format. It uses the database snapshot and existing identifier validators, reports missing/invalid fields and wrong pairings together, and has no automatic fix. Automated cases cover explicit arguments, missing database/CLI/schematic, duplicate references, invalid CSV, export failures, cancellation/cleanup, stable database snapshots, preservation of manufacturing files, and registration in the default configuration/fix workflow.

Real KiCad **10.0.3** checks passed against an isolated copy of the reference project (main schematic and three linked sheets) and the existing **1,060-pairing cache**. All copied `.kicad_pro`/`.kicad_sch` hashes remained unchanged after the passing check. KiCad itself created its usual `.kicad_prl` local-preferences file alongside the copied project. Removing MPN properties from the isolated `peripherals.kicad_sch` and hiding all saved table columns correctly failed with eight missing-MPN references: C13, C14, C27, C28, R7, R8, U5 and U7. No supplied project or manufacturing files were used as mutation targets.

The hidden GUI smoke and real production exports passed in `.local/cli-bom-check-ui-smoke`. The first report line now shows a green check when every result passes, or a red cross otherwise, including export errors and BOM/placement mismatches. Smoke assertions verify overall status position/colours, clearing stale status, the new CLI check, configuration fixes, and generation workflows. The screenshot `ui-smoke-status-symbols.png` was visually inspected. Build: `.local/cli-bom-check-build`; automated log: `.local/cli-bom-check-tests.log`. The MSI has not been rebuilt for this follow-up.

Split-check and report-symbol follow-up on **2026-09-11**: **197 tests pass**; the Release app build has zero warnings/errors in `.local/split-checks-build`. `BomIdentifiersCheck.cs` now reports missing/invalid BOM identifiers independently. Layout consistency checks footprint uniqueness, layout identifiers and comparisons of available values; incomplete BOM data produces a dependency note without repeating its missing-field list or falsely passing. Database pairing validation also refers to the new BOM-fields result for incomplete data. New regression cases reproduce missing MPNs on C1/C11 and verify that missing/duplicate footprints and other comparable fields are still checked. Existing read-only behavior and separate source files are retained.

GUI smoke and real KiCad production exports passed in `.local/split-checks-ui-smoke`. The report now uses `ReportTextBox.cs` (a read-only RichTextBox) to prefix every `[PASSED]` header with a green check and every `[FAILED]` header with a red cross. Smoke assertions verify native character colours, normal body text, report reset, the three identifier sections, and the existing configuration/generation workflows. The rendered status preview was inspected visually: symbols, wrapping, report scrolling and the existing window width/layout are correct. Evidence: `ui-smoke.txt`, `split-checks-report.txt`, `ui-smoke-status-symbols.png` and `ui-smoke-production.png` in that directory. The MSI has not been rebuilt for these changes.

Identifier check source follow-up on **2026-09-10**: **195 tests pass**, with zero build warnings/errors in `.local/identifier-checks-build`. Sixteen new tests cover accepted database alternatives, OEPS PN/OEPSPN aliases, whitespace and leading zeros, exact pairing comparisons, missing database/columns/values, conflicting aliases, duplicate BOM/PCB references, missing footprints, independent layout comparison, hidden and legacy PCB properties, KiCad's unquoted metadata keys, quoted/escaped field content, malformed/missing PCB files, grouped BOM rows with renamed CSV labels, stable database snapshots, cancellation, and retaining check results if a later export fails. Input bytes and absence of backups confirm that these checks do not modify project files. No configuration fixes are registered for them.

Real KiCad **10.0.3** export of an isolated reference-project copy (main schematic plus three linked sheets) produced a BOM with **29 components**. All 29 passed both identifier checks against the existing **1,060-pairing CSV cache** and their PCB footprints. The existing BOM/placement count mismatch remains independently reported (29 versus 37). Source and copied input hashes were checked; only the reference copy's required Edit tab settings were prepared before export. Evidence: `.local/bom-live-harness/identifiers-result.json`. Final GUI smoke in `.local/identifier-checks-ui-smoke` passed configuration, fix, selection, generation and preference checks, including the separate header-only PASS sections for both new checks. These source changes are not yet in the MSI or a published release.

BOM generation source follow-up on **2026-09-10**: **179 tests pass**; the Release app build has zero warnings/errors in `.local/bom-build`. The eleven new BOM tests cover saved field order/labels/grouping, Qty totals, reference differences in both directions, equal totals with different references, duplicate occurrences, empty BOMs, quoted CSV, stale output avoidance, preflight validation, failed/invalid output preservation, and retaining comparisons after later export failures. Existing selection tests now verify that the BOM is first in all 16 selection combinations, including BOM-only generation.

Real KiCad **10.0.3** verification used an isolated copy of the reference project, including its four schematic files. The reference's descending-sort setting was repaired in the copy using the existing Edit tab fix. BOM export produced **29 components**; placement export produced **37**. The comparison found no BOM-only references and these eight placement-only references: **H1, NT1, TP1, TP2, TP3, TP4, TP5, TP6**. SHA-256 checks confirmed that the supplied source files were unchanged and the CLI did not change its input copies. Evidence: `.local/bom-live-harness/result.json`. Explicit `--sort-asc true` crashed this installed CLI with `bad_any_cast`; the exporter now uses the default ascending order and requires that saved setting before cleanup.

Final GUI smoke in `.local/bom-ui-smoke-final` passed all checks using real CLI exports: automatic BOM first, empty sample BOM count 0 versus five placements, mismatch notification callback and report contents, busy controls, Gerber reminder cancellation, selective exports, preservation with cleanup disabled, BOM-only export, and no comparison with stale placement files. Native production notifications use a warning MessageBox; automated smoke substitutes a callback. An earlier run had a transient preferences-save assertion failure; the final run passed it with additional diagnostic detail available on failure. These source changes have not been packaged into the existing MSI or published.

Revision source follow-up: **168 tests pass** and the app builds with zero warnings/errors in `.local/silkscreen-revision-build`. Numeric revision fixes normalize the requested `ver`/`v` forms in both project and main schematic files. The new `PCB silkscreen revision` check accepts the supplied numeric/letter captions on F.SilkS or B.SilkS, rejects hidden text, other layers, metadata and partial version matches, and has no registered fix. GUI smoke in `.local/silkscreen-revision-ui-smoke` confirms failure reporting, no confirmation or PCB change, accurate status with no fix available, and successful recheck after manual correction. All four CLI export smoke checks also pass with the updated sample board. These source changes are not included in the MSI built below.

MSI and display-scaling verification on Windows on **2026-09-10**, for **0.1.1**:

- Release solution build: **zero warnings/errors; 162 tests passed**.
- Built `artifacts/Oeps.KicadProductionFiles-0.1.1-setup-win-x64.msi` (58,680,603 bytes) and the app update ZIP, each with a verified SHA-256 file. WiX 4.0.6 built the MSI with validation enabled.
- Decompiled the MSI and extracted its 488 payload files. Verified the per-user product/upgrade identity, both direct launcher shortcuts, HKCU registration, no custom actions or PowerShell files, and the bundled app checksum. The MSI carries only the host and .NET Core/Desktop 10.0.11 runtimes, with no SDK or ASP.NET runtime. Both the host and native app ran from the extracted payload.
- Ran the extracted app with the MSI runtime and isolated sample data. GUI checks and real KiCad CLI Gerber, placement, drill/map and IPC-D-356 exports all passed. This includes confirmations, cleanup, selection handling, report counts and busy controls. Evidence is in `.local/msi-inspection/verification.txt` and `.local/msi-inspection/native-app-smoke/ui-smoke.txt`.
- Simulated Windows DPI-change messages at 96, 120, 144 and 192 DPI, then back to 96; control containment, checkbox alignment, report separation and footer visibility passed. A fresh 560 x 500 client window passed scrolling/report-access checks. Screenshots were inspected; evidence is under `.local/layout-verification-final`. These are simulations on the current monitor, not physical multi-monitor tests.
- The operator's existing 0.1.0 app remained open and unchanged, so the native launcher's activation/installation path was not exercised against its real user data. Actual MSI install/upgrade/uninstall, mixed-DPI monitor movement and antivirus acceptance still require checks on target computers. The packages are unsigned. No GitHub release was published for 0.1.1 during this verification.

Earlier verification on Windows on 2026-09-08 with .NET SDK 10.0.400 from the sibling `raw-material-sticker` project. Earlier database, launcher and revision results below are retained from 2026-09-07.

- Release app/Core build: **zero warnings and zero errors**.
- Automated console suite: **162 passed, zero failed**. Includes twelve production generation tests covering all 16 export selection combinations, cleanup defaults/disabled cleanup, Gerber and drill CLI arguments, map requirements, optional Gerber job files, unsupported CLI preflight, malformed/empty/missing output, preservation of old output on failure, and cancellation. The eight placement tests were updated for temporary exports. Existing configuration, revision, data and updater tests also pass. The seven obsolete drill-profile tests and their check/fix were removed.
- Final production GUI smoke: **passed**, using `.local/production-exports-ui-smoke-final`. KiCad 10.0.3 generated three Gerber layers, a placement CSV with five components, separate PTH/NPTH Excellon files and two Gerber X2 drill maps. Confirmed right-side checkbox defaults, no generation with no types selected, Gerber reminder cancellation before cleanup, busy controls, output reporting, and placement-only generation without a reminder or deletion of existing Gerber/drill files. The old drill profile is neither initialized nor read. See `production-generation-preview.png`.
- Live spreadsheet: **1,060 OEPS PN/MPN pairings** downloaded and parsed. CSV saved to `.local/data/components.csv`, then successfully reloaded as an offline cache. The first sandbox HTTPS attempt failed; the permitted network retry succeeded.
- Installed KiCad CLI: `--version` returned **10.0.3**, exit code zero. Its netlist exporter successfully loaded the revised temporary schematic after the GUI fix.
- Placement GUI smoke with KiCad CLI 10.0.3: cleared nested obsolete manufacturing data on an isolated sample, generated one CSV with SMD, through-hole, DNP, Exclude from BOM, and bottom-side footprints. Confirmed millimeter offsets from the drill/place origin and positive bottom X. Generation locks inputs and action buttons, reports the relative output filename, and restores button state afterward. See `placement-preview.png`. The test board is `samples/placement-test.kicad_pcb`.
- Hidden GUI smoke: passed startup/button restrictions, successful/failed/repeated checks, input reset, generation override, refresh preserving state, saved paths/revision and layout. Revision mismatch and empty input fail; equivalent spellings pass. Fix workflow verification uses simulated Yes/No callbacks on an isolated sample project: No preserves the bytes and reports Skipped; Yes fixes the issue, creates a backup and automatically rechecks; passing checks do not prompt. Runtime confirmations use native Yes/No message boxes. See `revision-preview.png` and `configure-files-preview.png`.
- Previous working-project verification: all six project checks passed with expected revision `revB`. SHA-256 comparison confirmed the working PCB remained unchanged during the read-only check.
- Gerber plot-settings GUI smoke: reports a changed X2 setting as failed; No preserves PCB bytes, Yes restores only the changed value, creates a PCB backup and automatically rechecks. KiCad CLI then successfully exports placements from the repaired temporary PCB. See `gerber-settings-preview.png`. Zone-fill checking is explicitly outside the PCB-stored settings check.
- GUI preview inspected at the default **620 × 790 client size**, retaining the existing width and adding height for the generation options. The right-aligned stack does not overlap the report. An explicit 96-DPI design baseline supports scaling; higher-DPI and multi-monitor behavior still merit manual verification on the target workstation.
- Matching icon copied directly from `raw-material-sticker/assets/app.ico`.

The live CSV, diagnostic data and `.oeps-backups` directories are ignored by Git. The preview uses sample mode. BOM, Gerber, placement, drill and IPC-D-356 exports, BOM/placement comparison, and the BOM-field, database and layout identifier checks are implemented. Fix and generation verification changes only isolated test projects, not the user's supplied KiCad files or manufacturing directory.

- IPC-D-356 GUI verification: passed in `.local/ipcd356-ui-smoke`, including a real KiCad 10.0.3 export to `manufacturing/sample.d356` and IPC-only generation without a Gerber reminder. The sixth checkbox defaults to checked. All checkbox labels precede their controls, aligned on a common right edge; the updated preview was visually inspected. Release build: zero warnings/errors.
