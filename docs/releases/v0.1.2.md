OEPS KiCad Production Files for Windows x64.

- Added a Test descriptions button beside Report, with short explanations and fix availability in separate configuration and production tabs. Test names appear in bold.
- Added a read-only check for OEPS PN or MPN repeated across grouped BOM rows. It uses the shared CLI exporter with saved Symbol Fields Table grouping; the database check remains ungrouped.
- Included every grouping field in the temporary export so differences such as TempCo and Voltage keep rows separate. Multiple components within one grouped row are accepted.
- Duplicate reports now show the number of BOM rows and list each row's references separately.

Download **Oeps.KicadProductionFiles-0.1.2-setup-win-x64.msi** to install or upgrade. The **Oeps.KicadProductionFiles-0.1.2-win-x64.zip** is the launcher update package. Both downloads have SHA-256 checksum files.

Validated with 233 automated tests and a real KiCad CLI reproduction of grouping differences on an isolated project copy. Save the schematic and Symbol Fields Table settings in KiCad before checking.
